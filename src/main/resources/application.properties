

spring.datasource.url=jdbc:mariadb://localhost:3306/fila_online_db
spring.datasource.username=root
spring.datasource.password=123
# --- Configuracoes do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Configuracoles do Flyway ---
spring.flyway.locations=classpath:db/migration

# Ativa o perfil de desenvolvimento para exibir erros detalhados. "dev"(desativa notificacoes personalizadas)
# Para producao, remova ou comente esta linha, ou mude para 'prod'.
spring.profiles.active=dev